int EX7()
{
    int x,y,z,MAX;
    printf("saisir  les trois entiers:\n ");
    scanf("%d%d%d",&x,&y,&z);
if (x>y){
         MAX=x
         if (x>z)
            MAX=x
         else {MAX= }
         }
else {
    MAX=y
    if (y>z)
        MAX=y
    else{ MAX=z }
}
printf(la valeur la plus grand est :%d \n,MAX);

}
